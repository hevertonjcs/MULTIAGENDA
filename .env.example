# Exemplo de configuração de variáveis de ambiente para o Supabase.
#
# Copie este arquivo para `.env` ou `.env.local` e substitua os valores
# pelos dados da sua instância do Supabase antes de rodar ou implantar
# a aplicação. Estas variáveis serão utilizadas em tempo de build pelo Vite.

# URL base da instância do Supabase
VITE_SUPABASE_URL=<sua_url_do_supabase>

# Chave pública (anon) da instância do Supabase
VITE_SUPABASE_ANON_KEY=<sua_chave_anonima>